# mydhcp
a simple extensible dhcp server written in pure go. 

# dhcp4wich can be extended with plugins
At the very basic setup, its just a minimal implementation of a dhcp4 server
Plugins can enable different behaviour, for example a plugin could be log all dhcp requests for new IPS and write them to a log-file


